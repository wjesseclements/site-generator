{
  "name": "github-webhook",
  "version": "1.0.0",
  "description": "GitHub webhook endpoint for deployment status updates",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.1691.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.14.10",
    "typescript": "^5.5.3"
  }
}